---
---

<section class="problems">
  <div class="container problems-inner">

    <img src="/barra-superior.svg" class="problems-bar" alt="" aria-hidden="true" />

    <h2 class="problems-headline">
      Se qualquer um desses problemas soar<br />familiar, converse com a gente
    </h2>

    <div class="problems-grid">

      <article class="problem-card">
        <img src="/barra-lateral-card.svg" class="card-bar" alt="" aria-hidden="true" />
        <p>Processos críticos que dependem de pessoas específicas para funcionar.</p>
      </article>

      <article class="problem-card">
        <img src="/barra-lateral-card.svg" class="card-bar" alt="" aria-hidden="true" />
        <p>Decisões estratégicas tomadas com dados fragmentados ou desatualizados.</p>
      </article>

      <article class="problem-card">
        <img src="/barra-lateral-card.svg" class="card-bar" alt="" aria-hidden="true" />
        <p>Sistemas de diferentes empresas do portfólio sem integração.</p>
      </article>

      <article class="problem-card">
        <img src="/barra-lateral-card.svg" class="card-bar" alt="" aria-hidden="true" />
        <p>TI interno sobrecarregado, sem capacidade para projetos de inovação.</p>
      </article>

      <article class="problem-card">
        <img src="/barra-lateral-card.svg" class="card-bar" alt="" aria-hidden="true" />
        <p>Fornecedor anterior que entregou atrasado, abaixo do escopo ou sumiu.</p>
      </article>

      <article class="problem-card">
        <img src="/barra-lateral-card.svg" class="card-bar" alt="" aria-hidden="true" />
        <p>Produto e atuação digital que não reflete o porte da empresa.</p>
      </article>

    </div>

    <a href="/contato" class="btn-problems">Falar com a gente →</a>

  </div>
</section>

<style>
  .problems {
    background: #EBEBEB;
    padding: var(--space-9) 0;
  }

  .problems-inner {
    display: flex;
    flex-direction: column;
    align-items: center;
    text-align: center;
  }

  .problems-bar {
    display: block;
    width: 157px;
    height: auto;
    margin-bottom: var(--space-5);
  }

  .problems-headline {
    font-size: clamp(32px, 4vw, 56px);
    font-weight: 900;
    line-height: 1.05;
    letter-spacing: -0.02em;
    color: #0A0A0A;
    text-wrap: unset;
    margin-bottom: var(--space-8);
  }

  .problems-grid {
    display: grid;
    grid-template-columns: repeat(3, 1fr);
    gap: var(--space-4);
    width: 100%;
    text-align: left;
    margin-bottom: var(--space-8);
  }

  .problem-card {
    background: #fff;
    border: 0.2px solid #ffbda8;
    border-radius: var(--radius-lg);
    display: flex;
    align-items: center;
    gap: 20px;
    padding: var(--space-4);
  }

  .card-bar {
    width: 10px;
    height: 103px;
    flex-shrink: 0;
    display: block;
  }

  .problem-card p {
    font-size: var(--fs-body);
    font-weight: 500;
    color: #0A0A0A;
    line-height: 1.55;
  }

  .btn-problems {
    display: inline-flex;
    align-items: center;
    gap: 8px;
    padding: 15px 32px;
    background: #0A0A0A;
    color: #fff;
    font-size: var(--fs-body);
    font-weight: 600;
    border-radius: var(--radius-pill);
    border-bottom: none;
    transition: background var(--dur) var(--ease);
  }
  .btn-problems:hover {
    background: #212121;
    color: #fff;
  }

  @media (max-width: 900px) {
    .problems-grid { grid-template-columns: 1fr; }
  }
</style>
