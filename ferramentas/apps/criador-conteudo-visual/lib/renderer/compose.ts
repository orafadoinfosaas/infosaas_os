// Movido para @infosaas/renderer (fonte única, compartilhada com o preview do chat).
// Shim: mantém o caminho `@/lib/renderer/compose` vivo (composeFrame, RichSpec, BRAND_PAD…).
export * from '@infosaas/renderer'
