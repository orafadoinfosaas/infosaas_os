{
  "name": "@infosaas/cofre",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "description": "Cofre do Infosaas OS — tenants + tokens (hash) + credenciais cifradas (AES-256-GCM) no Postgres. Fonte única consumida pelo MCP (lê: token→tenant, secrets) e pelo painel (escreve: cria tenant, emite token, guarda secrets).",
  "main": "./dist/index.js",
  "types": "./dist/index.d.ts",
  "exports": {
    ".": {
      "types": "./dist/index.d.ts",
      "default": "./dist/index.js"
    }
  },
  "files": [
    "dist"
  ],
  "scripts": {
    "build": "tsc",
    "typecheck": "tsc --noEmit",
    "migrate": "node dist/migrate-cli.js"
  },
  "dependencies": {
    "pg": "^8.13.0"
  },
  "devDependencies": {
    "@types/node": "^22.0.0",
    "@types/pg": "^8.11.0",
    "typescript": "^5.6.0"
  }
}
