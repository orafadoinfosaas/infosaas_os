---
---

<section class="cta-ent">
  <div class="container cta-ent-inner">
    <img src="/barra-superior.svg" class="cta-ent-bar" alt="" aria-hidden="true" />

    <h2 class="cta-ent-headline">
      Projetos complexos começam com uma conversa
    </h2>

    <p class="cta-ent-body">
      Sem proposta genérica. O primeiro bate papo é para entender o negócio e os desafios reais.
    </p>

    <a href="/contato" class="btn-cta-ent">
      Falar com a gente →
    </a>
  </div>
</section>

<style>
  .cta-ent {
    background: var(--branco);
    padding: var(--space-10) 0;
  }

  .cta-ent-inner {
    display: flex;
    flex-direction: column;
    align-items: center;
    text-align: center;
  }

  .cta-ent-bar {
    display: block;
    width: 157px;
    height: auto;
    margin: 0 auto var(--space-5);
  }

  .cta-ent-headline {
    font-size: var(--fs-display);
    font-weight: 900;
    line-height: var(--lh-display);
    letter-spacing: var(--tr-tight);
    color: var(--preto);
    text-wrap: unset;
  }

  .cta-ent-body {
    font-size: var(--fs-body);
    color: var(--cinza-600);
    margin-top: var(--space-5);
    line-height: var(--lh-body);
    max-width: 480px;
  }

  .btn-cta-ent {
    display: inline-flex;
    align-items: center;
    gap: 6px;
    margin-top: var(--space-7);
    padding: 15px 32px;
    background: var(--laranja);
    color: #fff;
    font-size: var(--fs-body);
    font-weight: 600;
    border-radius: var(--radius-pill);
    border-bottom: none;
    transition:
      background var(--dur) var(--ease),
      box-shadow var(--dur) var(--ease);
  }
  .btn-cta-ent:hover {
    background: var(--laranja-press);
    box-shadow: var(--shadow-accent);
    color: #fff;
  }
</style>
