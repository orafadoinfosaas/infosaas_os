---
---

<section class="social-numbers">
  <div class="container">
    <div class="sn-inner">
      <img src="/barra-lateral-vertical-mono.svg" class="sn-bar" alt="" aria-hidden="true" />

      <div class="sn-content">

        <div class="sn-quote-block">
          <img src="/aspas.svg" class="sn-aspas" alt="" aria-hidden="true" />
          <h2 class="sn-headline">
            Funcionamos em uma rotação diferente do mercado.
          </h2>
          <p class="sn-body">
            A maioria dos nossos clientes chegaram com uma cicatriz:
            <strong>um projeto entregue com atraso, fora do escopo, ou uma empresa que sumiu
            depois do pagamento.</strong> Entendemos que a desconfiança do mercado nasce por
            causa de profissionais ruins. Por isso nossa rotação e forma de trabalho é diferente.
          </p>
        </div>

        <div class="sn-diferenciais">
          <div class="sn-dif">
            <h4>Equipe Fixa</h4>
            <p>
              Ao longo do atendimento você sabe exatamente quem vai trabalhar no projeto.
              O mesmo time do início ao fim — você sabe quem fala.
            </p>
          </div>
          <div class="sn-dif">
            <h4>Senioridade Evidente</h4>
            <p>
              Profissionais sêniors. Nenhum freelancer ou subcontratado aparece no meio do
              projeto por acaso.
            </p>
          </div>
          <div class="sn-dif">
            <h4>NPS Visceral Acima de 9.0</h4>
            <p>
              Não é número do marketing. É o que os clientes respondem quando perguntamos
              diretamente se indicariam a Infosaas®.
            </p>
          </div>
        </div>

        <div class="sn-metrics">
          <div class="sn-metric">
            <span class="sn-number">10 anos</span>
            <span class="sn-label">de experiência de mercado</span>
          </div>
          <div class="sn-metric">
            <span class="sn-number">10</span>
            <span class="sn-label">profissionais sêniors</span>
          </div>
          <div class="sn-metric">
            <span class="sn-number">R$ 60M+</span>
            <span class="sn-label">Receita gerada para clientes</span>
          </div>
          <div class="sn-metric">
            <span class="sn-number">9.2</span>
            <span class="sn-label">de NPS dos nossos clientes</span>
          </div>
        </div>

      </div>
    </div>
  </div>
</section>

<style>
  .social-numbers {
    background-image: url('/Rectangle-22.jpg');
    background-size: cover;
    background-position: center;
    padding: var(--space-9) 0;
    color: #fff;
  }

  .sn-inner {
    display: flex;
    align-items: flex-start;
    gap: 20px;
  }

  .sn-bar {
    width: 10px;
    height: auto;
    flex-shrink: 0;
    display: block;
  }

  .sn-content {
    flex: 1;
  }

  /* Quote block */
  .sn-quote-block {
    margin-bottom: var(--space-8);
  }

  .sn-aspas {
    display: block;
    width: 60px;
    height: auto;
    margin-bottom: var(--space-5);
  }

  .sn-headline {
    font-size: clamp(36px, 4vw, 56px);
    font-weight: 900;
    line-height: 1.05;
    letter-spacing: -0.02em;
    color: #fff;
    margin-bottom: var(--space-5);
  }

  .sn-body {
    font-size: var(--fs-body);
    color: rgba(255, 255, 255, 0.75);
    line-height: var(--lh-body);
    max-width: 700px;
  }
  .sn-body strong {
    color: #fff;
    font-weight: 600;
  }

  /* Diferenciais — mesma estrutura dos cards do About */
  .sn-diferenciais {
    display: grid;
    grid-template-columns: repeat(3, 1fr);
    border: 1.5px solid rgba(255, 255, 255, 0.35);
    border-radius: var(--radius-lg);
    overflow: hidden;
    margin-bottom: var(--space-8);
  }

  .sn-dif {
    padding: var(--space-6);
    display: flex;
    flex-direction: column;
    gap: var(--space-4);
  }
  .sn-dif:not(:last-child) {
    border-right: 1.5px solid rgba(255, 255, 255, 0.35);
  }

  .sn-dif h4 {
    font-size: var(--fs-h4);
    font-weight: 700;
    color: #fff;
    line-height: var(--lh-tight);
  }

  .sn-dif p {
    font-size: var(--fs-body-sm);
    color: rgba(255, 255, 255, 0.65);
    line-height: var(--lh-body);
  }

  /* Metrics */
  .sn-metrics {
    display: grid;
    grid-template-columns: repeat(4, 1fr);
    gap: var(--space-5);
  }

  .sn-metric {
    display: flex;
    flex-direction: column;
    gap: 6px;
  }

  .sn-number {
    font-size: clamp(26px, 3vw, 54px);
    font-weight: 900;
    line-height: 1;
    letter-spacing: -0.02em;
    color: #fff;
  }

  .sn-label {
    font-size: var(--fs-body);
    color: rgba(255, 255, 255, 0.6);
    line-height: 1.4;
  }

  @media (max-width: 900px) {
    .sn-bar { display: none; }
    .sn-diferenciais {
      grid-template-columns: 1fr;
      border-radius: var(--radius-lg);
    }
    .sn-dif:not(:last-child) {
      border-right: none;
      border-bottom: 1.5px solid rgba(255, 255, 255, 0.35);
    }
    .sn-metrics { grid-template-columns: repeat(2, 1fr); }
  }

  @media (max-width: 560px) {
    .sn-metrics { grid-template-columns: 1fr; }
  }
</style>
