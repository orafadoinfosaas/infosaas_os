---
---

<section class="cta-mvp">
  <div class="container cta-mvp-inner">
    <img src="/barra-superior.svg" class="cta-mvp-bar" alt="" aria-hidden="true" />

    <h2 class="cta-mvp-headline">
      Uma boa ideia não pode esperar por processos lentos
    </h2>

    <p class="cta-mvp-body">
      O MVP Ágil começa com uma conversa sobre o negócio, não sobre tecnologia.
    </p>

    <a href="/contato" class="btn-cta-mvp">
      Falar com a gente →
    </a>
  </div>
</section>

<style>
  .cta-mvp {
    background: var(--branco);
    padding: var(--space-10) 0;
  }

  .cta-mvp-inner {
    display: flex;
    flex-direction: column;
    align-items: center;
    text-align: center;
  }

  .cta-mvp-bar {
    display: block;
    width: 157px;
    height: auto;
    margin: 0 auto var(--space-5);
  }

  .cta-mvp-headline {
    font-size: var(--fs-display);
    font-weight: 900;
    line-height: var(--lh-display);
    letter-spacing: var(--tr-tight);
    color: var(--preto);
    text-wrap: unset;
  }

  .cta-mvp-body {
    font-size: var(--fs-body);
    color: var(--cinza-600);
    margin-top: var(--space-5);
    line-height: var(--lh-body);
    max-width: 480px;
  }

  .btn-cta-mvp {
    display: inline-flex;
    align-items: center;
    gap: 6px;
    margin-top: var(--space-7);
    padding: 15px 32px;
    background: var(--laranja);
    color: #fff;
    font-size: var(--fs-body);
    font-weight: 600;
    border-radius: var(--radius-pill);
    border-bottom: none;
    transition:
      background var(--dur) var(--ease),
      box-shadow var(--dur) var(--ease);
  }
  .btn-cta-mvp:hover {
    background: var(--laranja-press);
    box-shadow: var(--shadow-accent);
    color: #fff;
  }
</style>
